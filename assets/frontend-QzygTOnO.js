var e={"frontend-agent-interaction-safety-001":{terms:[{term:`AbortController`,meaning:`向支持取消的异步操作发送停止信号的控制器；fetch 需要接入它的 signal。`},{term:`竞态`,meaning:`多个请求交错完成，结果取决于先后时序，可能让旧数据覆盖当前页面。`}],example:{scenario:`第 7 题的解析请求 R7 尚未结束，用户已经切到第 8 题并发起 R8。`,steps:[`切题时把当前请求标识改为 R8，并调用 R7 的 abort，终止仍在进行的读取。`,`R7 已经排队的回调随后到达；处理器发现 R7 不等于当前 R8，直接忽略。`,`只有 R8 的有效事件能够追加到第 8 题解析中。`],conclusion:`取消减少无效工作，标识校验防止迟到事件改写状态，两者配合才覆盖这个时序问题；客户端取消也不等于服务端计算已自动停止。`}},"frontend-agent-interaction-safety-002":{terms:[{term:`XSS`,meaning:`跨站脚本攻击，指不可信内容被页面当成代码执行，获得该页面中的操作能力。`},{term:`白名单`,meaning:`明确列出允许出现的标签、链接协议或组件，未被允许的内容不执行。`}],example:{scenario:`模型复述了一段网页内容，其中夹带带 onerror 属性的图片标签和 javascript: 链接。`,steps:[`Markdown 渲染器关闭原始 HTML，把图片标签作为文字显示或忽略，避免生成带事件处理器的节点。`,`链接解析时只接受业务需要的协议，例如 https，拒绝 javascript:。`,`代码块交给只展示文本的组件，不把其中字符串交给 innerHTML。`],conclusion:`模型来源不能证明字符串安全；从解析、链接到组件都限制可执行能力，才能安全展示其中正常的 Markdown 内容。`}},"frontend-agent-interaction-safety-003":{terms:[{term:`SSE`,meaning:`服务器发送事件，通过持续的 HTTP 连接把事件推送给浏览器。`},{term:`幂等应用`,meaning:`同一个事件处理一次或重复处理多次，对界面的最终影响保持相同。`},{term:`事件序号`,meaning:`应用给同一会话的事件分配的顺序编号，用来识别重复和缺口。`}],example:{scenario:`会话 C1 已显示到第 12 段，断线重连后服务器重放第 12、13 段。`,steps:[`客户端保存 C1 最后成功应用的位置 12；服务端事件使用稳定 ID。`,`再次收到 C1/12 时发现已处理，跳过追加；C1/13 才追加并更新位置。`,`若下一条直接是 C1/15，则补取第 14 段，不能把缺口当成正常完成。`],conclusion:`稳定身份让重放可识别，应用记录保证内容不重复；原生重连的 Last-Event-ID 只是恢复线索，不能代替业务去重。`}},"frontend-agent-interaction-safety-004":{terms:[{term:`readyState`,meaning:`EventSource 当前连接状态：CONNECTING 表示连接或重连中，OPEN 表示已连接，CLOSED 表示已关闭且不再重连。`},{term:`error 事件`,meaning:`连接出现问题时的通知，既可能伴随重连，也可能表示连接已终止。`}],example:{scenario:`答案生成到一半时网络短暂断开，原生 EventSource 触发 error。`,steps:[`处理器读取 readyState，发现为 CONNECTING，把提示改成“正在重连”并保留已生成内容。`,`连接恢复后收到 open，状态改成 OPEN，继续接收片段。`,`收到业务完成事件后调用 close，此时 CLOSED 才表示本次连接不再尝试恢复。`],conclusion:`错误通知与连接终态不是一一对应；结合状态显示才能解释当前进度，也避免重连时重复创建连接。`}},"frontend-agent-interaction-safety-005":{terms:[{term:`背压`,meaning:`消费方跟不上时，向生产或传输环节反馈压力，让数据流入速度降低。`},{term:`有界队列`,meaning:`待处理消息缓存设有容量上限，接近上限就触发过载处理。`},{term:`恢复游标`,meaning:`标记最后已处理位置的数据，服务端可据此继续提供后续消息。`}],example:{scenario:`生成服务每秒推来 200 段，页面每秒只能处理 100 段，队列不断积压。`,steps:[`把消息按帧合并绘制，并为队列设置容量和提前触发的高水位。`,`达到高水位时请求服务端降速；协议不支持时关闭连接，保留最后已应用序号 800。`,`积压处理完成后，按已更新的游标重新连接，服务端补发后续片段。`],conclusion:`稳定 WebSocket 没有接收背压，少绘制不能自动减少收包；有界缓存与可恢复协议要一起设计。`}},"frontend-agent-interaction-safety-006":{terms:[{term:`闭包`,meaning:`函数与其定义处的外层变量环境形成的联系，函数在外层返回后仍能访问这些变量。`},{term:`变量绑定`,meaning:`变量名与当前值之间的联系；这里的 count 绑定会随递增被更新。`}],example:{scenario:`makeCounter 内创建 count = 0，返回 () => ++count；执行一次 makeCounter 得到 counter。`,steps:[`第一次调用 counter，找到外层保留的 count，把 0 改为 1；前置递增返回 1。`,`第二次调用同一个 counter，读取已变成 1 的绑定，再改为 2 并返回 2。`,`另调用一次 makeCounter 得到 other，它拥有另一份初始 count，首次调用返回 1。`],conclusion:`初始化次数由工厂调用次数决定；同一闭包连续操作同一绑定，所以题中依次得到 1、2。`}},"frontend-agent-interaction-safety-007":{terms:[{term:`const`,meaning:`创建不能重新赋值的变量绑定，但不自动限制绑定所指对象内部的属性。`},{term:`对象属性`,meaning:`对象中按名称保存的值，例如 user.name；普通属性可以单独改写。`}],example:{scenario:`个人资料页执行 const user = { name: 'Lin' }，随后把姓名改为 Lee。`,steps:[`user.name = 'Lee' 修改现有对象的 name，user 仍指向同一个对象。`,`读取 user.name 得到 Lee；const 并没有给 name 自动添加只读限制。`,`若执行 user = { name: 'Chen' }，则是在更换 user 的绑定值，会违反 const 规则。`],conclusion:`判断赋值是否合法，要看改的是绑定还是属性；这里修改普通属性合法。React 等状态管理还可能另有不可变更新约定。`}},"frontend-agent-interaction-safety-008":{terms:[{term:`严格相等 ===`,meaning:`比较时不自动转换类型；不同类型直接判为不相等，再按对应类型的规则比较。`},{term:`NaN`,meaning:`表示无法得到有效数字的特殊数值；它使用 === 与自身比较也不相等。`}],example:{scenario:`输入框返回字符串 '0'，程序把它与数字 0 比较；另一次数字转换得到了 NaN。`,steps:[`计算 0 === '0'，一边是 number，一边是 string，因此结果为 false。`,`计算 NaN === NaN，按特殊数值的比较规则，结果仍为 false。`,`若业务要识别 NaN，应执行 Number.isNaN(value)，而不是 value === NaN。`],conclusion:`两次比较失败的原因不同：前者类型不同，后者是 NaN 的规则，所以答案是 false、false，不能按显示文字相同来判断。`}},"frontend-agent-interaction-safety-009":{terms:[{term:`空值合并 ??`,meaning:`仅当左边是 null 或 undefined 时取右边的默认值。`},{term:`假值`,meaning:`转成布尔值为 false 的值，包括 0、空字符串、false、null 等；范围比空值更大。`}],example:{scenario:`分页设置中，0 代表暂不展示，未填写则用默认页容量 20。`,steps:[`用户主动设置 pageSize = 0，计算 pageSize ?? 20 得到 0，界面按约定隐藏列表。`,`设置缺失为 undefined 或明确为空的 null 时，同一表达式才得到 20。`,`若改用 pageSize || 20，合法的 0 会触发默认值，页面反而显示 20 条。`],conclusion:`先按业务区分“有意设为零”和“没有提供”，再选择只处理空值的 ??；若业务对假值的含义不同，默认规则也要相应改变。`}},"frontend-agent-interaction-safety-010":{terms:[{term:`map`,meaning:`对数组中存在的元素逐一调用函数，用每次返回值组成新数组。`},{term:`箭头函数返回值`,meaning:`表达式函数体可隐式返回；使用花括号的块函数体，需要显式写 return。`}],example:{scenario:`要把两件商品的积分 [1, 2] 翻倍，代码写成 [1, 2].map(n => { n * 2; })。`,steps:[`处理 1 时计算了 2，但块内没有 return，函数实际返回 undefined。`,`处理 2 时同样只计算、不返回，因此新数组是 [undefined, undefined]，长度仍为 2。`,`改成 n => n * 2 或在块内写 return n * 2，结果才是 [2, 4]。`],conclusion:`map 收集的是回调返回值，不是最后一行计算痕迹；漏写 return 既不会保留原值，也不会像筛选那样移除元素。`}},"frontend-agent-interaction-safety-011":{terms:[{term:`当前任务`,meaning:`浏览器正在执行的一段工作，其中的同步代码通常连续运行到调用栈清空。`},{term:`微任务`,meaning:`当前任务结束后在检查点处理的回调，已兑现 Promise 的 then 会进入这一队列。`}],example:{scenario:`同一段脚本依次打印 A、安排零毫秒定时器 B、安排 Promise 回调 C，再打印 D。`,steps:[`同步执行先打印 A；安排 B、C 时只是登记回调，随后立即打印 D。`,`当前脚本执行完毕，处理微任务队列，Promise 的回调打印 C。`,`之后轮到已满足等待条件的定时器任务，才打印 B。`],conclusion:`输出为 A、D、C、B。setTimeout 的 0 不表示马上插入当前代码，Promise 已兑现也不使 then 变成同步回调。`}},"frontend-agent-interaction-safety-012":{terms:[{term:`Promise.all`,meaning:`汇总多个异步结果：全部成功才得到结果数组，任意输入拒绝就使聚合结果拒绝。`},{term:`取消操作`,meaning:`通过底层支持的机制停止仍在进行的工作，与报告 Promise 失败是不同动作。`}],example:{scenario:`页面同时发起用户资料请求 A 和报表请求 B，并用 Promise.all 等待。`,steps:[`A 因网络错误在 100ms 时拒绝，聚合 Promise 随即拒绝，页面可以进入错误处理。`,`B 原本需要 2 秒；Promise.all 没有自动发送取消信号，B 仍可继续完成。`,`若业务要求 A 失败就停止 B，需要另外对接给 B 的 AbortController 调用 abort。`],conclusion:`快速失败约束的是汇总结果，不改变已启动请求的生命周期；要全部收集用相应汇总策略，要停止资源则明确执行取消。`}},"frontend-agent-interaction-safety-013":{terms:[{term:`边框盒`,meaning:`包含内容、内边距和边框的矩形区域，不包含外边距 margin。`},{term:`box-sizing`,meaning:`指定 width 等尺寸约束应用于内容盒还是边框盒的 CSS 属性。`}],example:{scenario:`卡片宽度声明为 200px，左右各有 20px 内边距和 2px 边框，并使用 border-box。`,steps:[`border-box 先确定边框盒总宽为声明的 200px，不再把内边距和边框加到外面。`,`内部内容可用宽度为 200 - 20 × 2 - 2 × 2 = 156px。`,`若另加左右各 10px 的 margin，外部占用还需计入这 20px，但边框盒仍为 200px。`],conclusion:`题目问边框盒总宽，因此是 200px。244px 对应无其他约束时 content-box 的计算方式，不能套到这里。`}},"frontend-agent-interaction-safety-014":{terms:[{term:`flex-shrink`,meaning:`分配 Flex 收缩量的系数，不能突破最小尺寸约束。`},{term:`自动最小宽度`,meaning:`min-width 为 auto 时按布局条件计算的下限，非滚动 Flex 子项常受内容最小宽度影响。`}],example:{scenario:`300px 横向容器中，按钮占 80px，标题是一串不能换行的文本。`,steps:[`标题虽设 flex-shrink: 1，内容决定的自动最小宽度仍可能超过余下空间。`,`给标题所在 Flex 子项设 min-width: 0，允许盒子继续缩小。`,`如果设计要求省略显示，再配合 overflow: hidden、white-space: nowrap 和 text-overflow: ellipsis。`],conclusion:`解除最小宽度解决盒子能否变窄，溢出样式决定文字怎样显示；仅增加收缩系数无法完成这两件事。`}},"frontend-agent-interaction-safety-015":{terms:[{term:`原生按钮`,meaning:`HTML 的 button 元素，自带按钮语义、可聚焦能力和键盘激活行为。`},{term:`按钮 type`,meaning:`决定按钮是提交表单还是执行普通操作；type='button' 表示不以提交作为默认行为。`}],example:{scenario:`答题表单有“提交答案”和“展开提示”，后者只展开说明。`,steps:[`把提示控件写成 <button type='button'>展开提示</button>，点击时只切换展开状态。`,`键盘用户用 Tab 聚焦，再按 Enter 或空格，也能触发按钮。`,`真正提交的控件另设 type='submit'；若提示按钮省略 type，表单内可能意外提交。`],conclusion:`原生 button 提供完整的基础交互，显式类型区分操作意图；单给 div 或 span 加 tabindex，只会变得可聚焦，不会自动具备按钮行为。`}},"frontend-agent-interaction-safety-016":{terms:[{term:`同源`,meaning:`通常指协议、主机名和端口都相同，是网页存储隔离的重要边界。`},{term:`标签页会话`,meaning:`一个标签页对应的页面会话；sessionStorage 还按这个范围隔离，普通刷新不结束它。`}],example:{scenario:`同一答题站点用 localStorage 保存主题，用 sessionStorage 保存当前标签页的草稿。`,steps:[`在标签页 A 保存 dark 主题和草稿后刷新，正常情况下两种数据都还在。`,`另一个同源标签页修改自己的 sessionStorage，不会持续同步改写 A 的草稿。`,`结束页面会话后，sessionStorage 不用于下次长期恢复；localStorage 通常可跨浏览器会话保留。`],conclusion:`两者区别在存储作用范围与生命周期，不是刷新是否清空；浏览器策略或用户清理仍可删除数据，都不能视为永久保存。`}},"frontend-agent-interaction-safety-017":{terms:[{term:`no-cache`,meaning:`允许缓存保存响应，但再次使用前必须向服务器验证其有效性。`},{term:`no-store`,meaning:`要求缓存不要保存该请求或响应，解决的是能否存储的问题。`}],example:{scenario:`公告页内容经常变化，允许浏览器保存正文，但每次复用前必须确认是否有新版。`,steps:[`服务器返回 Cache-Control: no-cache，并附上可用于验证的 ETag。`,`下次访问时浏览器带着已有验证器向服务器确认；未变化时可复用本地正文。`,`如果内容变化，就接收新正文和验证器，而不是继续直接显示旧副本。`],conclusion:`题目既允许保存又要求每次验证，所以选择 no-cache；no-store 会禁止保存，只有 max-age 则可能在新鲜期内跳过验证。`}},"frontend-agent-interaction-safety-018":{terms:[{term:`CORS`,meaning:`服务器通过响应头告诉浏览器，哪些源的脚本可以读取跨源响应的机制。`},{term:`凭据模式`,meaning:`控制请求是否使用 Cookie 等凭据的选项；include 也受 Cookie 策略限制。`}],example:{scenario:`答题站向跨源的用户接口发送凭据模式为 include 的请求。`,steps:[`服务端检查 Origin，确认它在可信源名单中。`,`返回 Access-Control-Allow-Origin: https://quiz.example 及 Access-Control-Allow-Credentials: true。`,`按 Origin 动态返回时设置 Vary: Origin，让缓存区分不同来源的响应。`],conclusion:`凭据请求需明确授权源和凭据许可，不能用 *。CORS 不代替登录验证，也不保证 Cookie 一定发送。`}},"frontend-agent-interaction-safety-019":{terms:[{term:`unknown`,meaning:`可接收任意值，但在使用具体属性或方法前必须先证明相应类型的 TypeScript 类型。`},{term:`类型缩小`,meaning:`根据条件检查，把可能的类型范围缩到当前操作所支持的范围。`}],example:{scenario:`页面接收外部消息，payload 可能是字符串，也可能是数字或对象。`,steps:[`把 payload 声明为 unknown，直接调用 payload.trim() 会被静态检查阻止。`,`先判断 typeof payload === 'string'，在该分支内调用 trim，才符合字符串方法的前提。`,`若需要 User 对象，还要检查它的字段结构，不能只把变量名改成 user 就信任内容。`],conclusion:`unknown 把验证责任放到使用之前；any 则会放行许多未经证明的操作。两种类型都不会自动在运行时验证网络数据。`}},"frontend-agent-interaction-safety-020":{terms:[{term:`联合类型`,meaning:`用 | 表示一个值可能属于多个类型，例如 string | number。`},{term:`类型守卫`,meaning:`能帮助判断运行时类型的条件，让编译器在相应分支缩小值的类型范围。`}],example:{scenario:`value: string | number 中，字符串要转大写，数字保持原值。`,steps:[`输入 'lin' 时，typeof value === 'string' 成立，该分支中调用 value.toUpperCase() 得到 LIN。`,`输入 12 时，条件不成立，走数字分支，不调用不存在的字符串方法。`,`如果只是写 value as string，12 在运行时仍是数字，并没有完成字符串转换。`],conclusion:`真正排除 number 的 typeof 条件才能支持安全调用；非空检查无法区分这两个成员，类型断言也不能替代运行时判断。`}},"frontend-agent-interaction-safety-021":{terms:[{term:`key`,meaning:`React 用来在兄弟列表项之间识别组件身份、跨渲染匹配同一项的标识。`},{term:`组件状态`,meaning:`组件实例保留的数据，例如编辑框里的草稿；它需要与对应记录身份保持一致。`}],example:{scenario:`列表原有 id 为 A、B 的两条记录，B 的编辑框里已经输入一段未保存草稿。`,steps:[`在头部插入 C 后，位置从 [A, B] 变成 [C, A, B]。`,`使用 key={item.id} 时，React 仍能匹配 B，草稿继续归属于 B。`,`若用下标，原位置 1 现在是 A，位置身份与业务记录不再一致；随机 key 则会使身份反复变化。`],conclusion:`需要保持的是同一条数据的状态，所以使用稳定且在兄弟间唯一的 id；key 不必全站唯一，但不能随着排序或每次渲染改变。`}},"frontend-agent-interaction-safety-022":{terms:[{term:`状态快照`,meaning:`一次 React 渲染中读取到的状态值，在该次渲染的事件处理器里不会被 setter 当场改写。`},{term:`函数式更新`,meaning:`向 setter 传入纯函数，由 React 用队列处理后的前一个状态计算下一个状态。`}],example:{scenario:`计数器显示 0，用户点击一次“加三”，处理器连续安排三次加一。`,steps:[`三次调用 setCount(c => c + 1)，分别向队列加入一个加一变换。`,`处理队列时，第一个把 0 变 1，第二个接收 1 变 2，第三个接收 2 变 3。`,`若三次都传 count + 1，它们读取同一份 count=0 快照，提交的值都为 1。`],conclusion:`函数式更新表达连续变换，才明确依赖队列中的前值；更新器应保持纯净，不在其中发请求或修改外部数据。`}},"frontend-agent-interaction-safety-023":{terms:[{term:`ref`,meaning:`Vue 用来包装响应式值的对象，通过它的 value 属性读取和修改内部值。`},{term:`自动解包`,meaning:`例如模板中的顶层 ref 会自动读取内部值，普通脚本不能套用此规则。`}],example:{scenario:`Vue 3 setup 中有 const count = ref(0)，点击按钮要加一。`,steps:[`脚本中读取 count.value 得到数字 0，而 count 本身是包装这个数字的 ref 对象。`,`执行 count.value++，内部值变成 1，依赖该值的界面可以随响应式更新变化。`,`模板顶层展示 count 时可利用自动解包，但把脚本写成 count++ 并不会沿用模板规则。`],conclusion:`脚本通过 .value 读写 ref；const 限制包装对象的绑定重新赋值，不阻止修改内部值。`}},"frontend-agent-interaction-safety-024":{terms:[{term:`尾沿防抖`,meaning:`连续触发时不断重新计时，只有最后一次触发后安静满指定时长才执行。`},{term:`节流`,meaning:`限制一段时间内的执行频率，持续触发时也可能定期执行，与等待停止输入不同。`}],example:{scenario:`用户在 0ms 输入 r、100ms 输入 re、220ms 输入 rea，随后停止，防抖时间为 300ms。`,steps:[`第一次安排 300ms 时搜索 r；第二次取消该定时器，改为 400ms 搜索 re。`,`第三次再次取消，改为 520ms 搜索最新值 rea；期间没有新输入，最终只发这一请求。`,`若每个按键独立延迟，300、400、520ms 都会请求，调用没有被合并。`],conclusion:`关键是取消旧定时器并用最新输入重新等待；防抖控制发送频率，已经发出的旧请求仍需单独防止结果乱序覆盖。`}},"frontend-agent-interaction-safety-025":{terms:[{term:`Effect`,meaning:`React 用来让组件与订阅、连接等外部系统保持同步的机制。`},{term:`清理函数`,meaning:`Effect 返回的函数，用于撤销这一次创建的订阅或资源，在依赖切换和卸载时执行。`}],example:{scenario:`聊天页首次 roomId 为 A，Effect 订阅 A；用户随后切到房间 B。`,steps:[`把 roomId 放进依赖数组，A 的 Effect 返回取消 A 订阅的函数。`,`roomId 变为 B 后，React 先清理旧的 A 订阅，再执行新 Effect 订阅 B。`,`页面卸载时清理 B；即使开发环境额外检查创建和清理流程，也不应积累连接。`],conclusion:`依赖确保房间变化会触发同步，清理确保旧资源被释放。只改变量或覆盖显示内容，不会停止旧房间继续发消息。`}},"frontend-agent-interaction-safety-026":{terms:[{term:`不可变更新`,meaning:`保留旧状态不被改写，创建包含变化的新对象作为下一份状态。`},{term:`浅复制`,meaning:`对象展开只复制这一层属性，嵌套对象仍共享引用，需按修改路径继续复制。`}],example:{scenario:`状态为 { profile: { name: 'Lin', city: '北京' }, theme: 'dark' }，只把城市改成上海。`,steps:[`用 ...prev 复制外层，保留 theme；为 profile 创建新对象。`,`在新 profile 中用 ...prev.profile 保留 name，再写 city: '上海'。`,`新状态拥有新外层与新 profile，旧状态的 city 仍为北京，其他字段没有丢失。`],conclusion:`逐层复制修改路径并保留同层属性。useState 替换对象，不自动合并字段；原地修改并复用旧引用会破坏快照。`}},"frontend-agent-interaction-safety-027":{terms:[{term:`useMemo`,meaning:`React 对计算结果进行缓存的性能工具；缓存可以在特定情况下被丢弃。`},{term:`业务状态`,meaning:`正确性必须依赖的数据，例如用户输入的草稿，不能因优化缓存重建而意外丢失。`}],example:{scenario:`团队把未保存草稿对象只放在 useMemo(() => ({ text: '' }), []) 中，并期望它永远存在。`,steps:[`空依赖使普通重渲染通常复用对象，但没有建立永久保留的业务承诺。`,`例如首次挂载发生挂起时，React 可以丢弃计算缓存，后续计算会创建新的对象。`,`驱动界面的草稿用 state 管理；需要跨卸载恢复时，再把数据提升或保存到合适存储。`],conclusion:`正确性应由数据生命周期设计保障，useMemo 只用于减少重复计算；state 或 ref 也不会自动让对象跨组件卸载永久存在。`}},"frontend-agent-interaction-safety-028":{terms:[{term:`computed`,meaning:`Vue 根据响应式依赖计算派生值并缓存结果的机制，适合纯计算。`},{term:`副作用`,meaning:`除了返回计算值以外的外部变化，例如请求接口、写入存储或修改其他状态。`}],example:{scenario:`购物车包含单价 10 元、数量 2 的商品，同时有一个会改变的商品搜索词。`,steps:[`computed 读取单价与数量，返回总价 20；数量改为 3 后，依赖变化使总价重新计算为 30。`,`另用 watch 观察搜索词；词从笔记本变成键盘时，发起对应的商品查询。`,`读取总价只得到数值，不应顺带发送搜索请求或修改购物项。`],conclusion:`总价是可从现有数据推导的值，请求是需要控制生命周期的操作。按这个职责划分，缓存读取才不会悄悄触发业务副作用。`}},"frontend-agent-interaction-safety-029":{terms:[{term:`props`,meaning:`父组件传给子组件的输入，表达由父组件持有和维护的数据。`},{term:`单向数据流`,meaning:`值从父组件流向子组件，子组件用事件报告修改意图，再由父组件更新源数据。`}],example:{scenario:`父组件持有 title='面试笔记'，子组件提供标题输入框，用户将它改为前端笔记。`,steps:[`子组件接收 title，用它展示当前标题；编辑产生的新值由子组件发出 update:title 事件。`,`父组件收到事件后修改自己保存的 title，新值再次通过 props 传给子组件。`,`也可按组件 v-model 协议连接这条更新路径，而不是直接给 props.title 赋值。`],conclusion:`源数据始终由拥有它的父组件修改，更新路径才可追踪。子组件若维护可取消的草稿，需要明确何时提交，复制一份值不会自动回写。`}},"frontend-agent-interaction-safety-030":{terms:[{term:`泛型 T`,meaning:`用一个类型参数关联输入和输出，让调用时的具体类型继续保留。`},{term:`泛型约束`,meaning:`用 extends 声明输入必须具备的最小结构，例如具有数字类型的 length 属性。`}],example:{scenario:`keep 函数需要打印输入长度，再原样返回输入；调用者传入一个字符串数组。`,steps:[`签名写为 keep<T extends { length: number }>(value: T): T，函数体可以访问 value.length。`,`传入 ['a', 'b'] 时，T 推断为字符串数组，返回值仍可使用数组的 map 等方法。`,`传入数字 42 时，因为不具备 length，静态检查会指出不满足约束。`],conclusion:`约束保证能力，共享 T 保留具体类型。若返回类型仅为 { length: number }，便丢失数组类型信息。`}},"frontend-agent-interaction-safety-031":{terms:[{term:`类型断言`,meaning:`用 as 告诉 TypeScript 按某类型看待值，不执行转换，也不生成字段校验代码。`},{term:`运行时验证`,meaning:`实际检查收到的数据，例如 name 是否为字符串。`}],example:{scenario:`User 要求 name 是字符串，但接口误返回合法 JSON：{ name: 12 }。`,steps:[`response.json() 能成功解析，因为数字字段没有违反 JSON 语法。`,`写 as User 后，编译器按 User 看待结果，运行时 name 仍是 12；调用 name.toUpperCase() 依然可能报错。`,`在进入业务代码前先验证对象结构和字段类型，失败时显示数据异常，通过后才使用。`],conclusion:`解析成功、静态类型承诺和真实结构正确是不同条件。as 与 strict 都不能自动替代接口边界的运行时校验。`}},"frontend-agent-interaction-safety-032":{terms:[{term:`CSRF`,meaning:`跨站请求伪造，诱导浏览器利用会话执行未经用户授权的操作。`},{term:`CSRF token`,meaning:`随合法操作提交的防伪值，由服务端校验，补充仅凭 Cookie 的检查。`},{term:`SameSite`,meaning:`Cookie 在跨站请求中是否发送的策略，可减少部分 CSRF 场景，但有部署和请求类型边界。`}],example:{scenario:`用户登录资料站后打开外站，外站尝试代用户提交修改昵称的请求。`,steps:[`若浏览器在该场景携带会话 Cookie，仅凭登录状态可能接受这次修改。`,`服务端检查防伪 token 或可靠的可信来源信号，缺失或不符时拒绝操作。`,`再配置适合业务的 SameSite，并保证 GET 不修改资料。`],conclusion:`防护要判断操作来源与授权意图；HttpOnly 只限制脚本读 Cookie，CORS 主要限制响应读取，都不能单独替代这些检查。`}},"frontend-agent-interaction-safety-033":{terms:[{term:`defer`,meaning:`外部经典脚本的延迟执行属性，可在解析 HTML 时下载，解析完成后按文档顺序执行。`},{term:`async`,meaning:`外部脚本就绪后尽快执行的属性，不保证多个脚本按文档先后执行。`}],example:{scenario:`HTML 先引用创建工具对象的 A.js，再引用使用该对象的 B.js，且 B 下载得更快。`,steps:[`两者都声明 defer，浏览器下载脚本的同时继续解析文档。`,`即使 B 先下载完，也按 A、B 的文档顺序执行，因此 B 使用时 A 已创建工具对象。`,`若都设 async，B 可能先执行，依赖对象尚未存在，顺序便没有保障。`],conclusion:`题设要同时满足解析不中断与依赖顺序，使用按 A、B 声明的 defer。这个推导限定于解析器插入的外部经典脚本，不直接套用动态或模块脚本。`}},"frontend-agent-interaction-safety-034":{terms:[{term:`强制同步布局`,meaning:`修改样式后立即要求最新尺寸，浏览器为回答读取而当场重新计算布局。`},{term:`布局抖动`,meaning:`频繁交错修改与测量，使浏览器反复计算布局，增加主线程耗时。`}],example:{scenario:`页面要把 100 张独立卡片改成相同宽度，并记录每张卡片修改后的高度。`,steps:[`原循环每改一张宽度就读 offsetHeight，可能每轮都迫使浏览器计算新布局。`,`在卡片间没有中间测量依赖的前提下，先完成全部宽度写入，再集中读取更新后的高度。`,`用性能工具检查布局次数和耗时；若后一步依赖前一步的测量结果，则保留必要的计算顺序。`],conclusion:`优化目标是合并必要的布局计算，同时不改变测量语义；把原循环放进 requestAnimationFrame，并不会自动消除内部读写交错。`}},"frontend-agent-interaction-safety-035":{terms:[{term:`React.lazy`,meaning:`把组件代码的加载推迟到首次需要渲染时；加载模块尚未就绪时会等待。`},{term:`Suspense`,meaning:`在子组件等待受支持的异步资源时展示 fallback 占位内容的边界。`},{term:`错误边界`,meaning:`捕获子树渲染错误并展示备用界面的 React 机制，可以处理 lazy 导入拒绝。`}],example:{scenario:`用户第一次打开报表页，Report 模块还没有下载，之后下载可能失败。`,steps:[`在组件外稳定声明 lazy 组件，路由首次渲染它时触发 import。`,`下载尚未完成时，Suspense 显示“正在加载报表”；成功后替换为报表内容。`,`若导入 Promise 拒绝，由外层错误边界显示失败说明和合适的恢复入口。`],conclusion:`等待与失败是不同状态，需要各自的处理机制；单有 Suspense 不会自动把导入失败转换成可用的重试逻辑。`}},"frontend-agent-interaction-safety-036":{terms:[{term:`Response`,meaning:`fetch 收到的 HTTP 响应对象，包含状态码、响应头和正文。`},{term:`response.ok`,meaning:`状态码在 200 到 299 时为 true，用于检查 HTTP 是否成功。`}],example:{scenario:`请求不存在的文章，服务器返回 HTTP 404 和 JSON 错误说明。`,steps:[`await fetch 得到 Response，并不会仅因 404 自动跳到 catch。`,`检查 response.ok 发现为 false，再根据 status=404 展示“文章不存在”，或主动抛出由上层处理的错误。`,`即使错误正文能成功解析成 JSON，也不意味着文章请求成功。`],conclusion:`catch 处理的是拒绝或主动抛出的异常；HTTP 状态需要显式检查。网络错误、HTTP 错误和业务错误不应混成一个判断。`}},"frontend-agent-interaction-safety-037":{terms:[{term:`ETag`,meaning:`服务器提供的资源验证标识，判断客户端副本是否仍有效。`},{term:`条件请求`,meaning:`请求附带已有副本的验证条件，例如 If-None-Match，让服务器决定是否需要重传。`},{term:`304`,meaning:`GET 等条件验证中表示资源未修改的状态，客户端可继续使用已有正文。`}],example:{scenario:`浏览器保存了 200KB 的题库文件以及 ETag "q-v4"，现在需要向服务器验证。`,steps:[`GET 请求携带 If-None-Match: "q-v4"，告知服务端本地副本的验证标识。`,`服务器确认资源未变化，返回 304，不重传完整的 200KB 正文。`,`客户端复用之前保存的正文；若资源已变化，则通常收到 200 和新版内容。`],conclusion:`304 节省的是正文传输，前提是确实持有可用副本；ETag 本身不是内容，无法重建一个从未保存过的文件。`}},"frontend-agent-interaction-safety-038":{terms:[{term:`事件委托`,meaning:`把监听器设在父容器上，利用子元素事件向上冒泡统一处理动态子项。`},{term:`target / currentTarget`,meaning:`target 是事件最初命中的目标；currentTarget 是当前执行这个监听器的元素。`}],example:{scenario:`列表容器监听点击，每行的删除按钮里有一个图标，用户实际点中了图标。`,steps:[`event.target 可能是图标，event.currentTarget 则是绑定监听器的列表容器。`,`确认 target 是 Element 后，调用 closest('button[data-action]') 找到所属按钮。`,`再确认按钮属于当前列表容器，才读取行标识并执行对应操作。`],conclusion:`从最初目标向上定位并限制作用范围，才能兼容嵌套图标；停止冒泡不会自动把 target 改成按钮。`}},"frontend-agent-interaction-safety-039":{terms:[{term:`Tree shaking`,meaning:`构建时根据导入、导出和副作用信息，去除不需要的代码或模块。`},{term:`sideEffects`,meaning:`向 webpack 声明导入时有副作用的文件；CSS 应用样式也属于副作用。`}],example:{scenario:`Button.js 导入 Button.css，但生产构建后按钮样式消失。`,steps:[`检查 package.json，发现 sideEffects: false，把包含样式导入在内的文件都声明成可忽略副作用。`,`改为准确名单，例如 sideEffects: ['**/*.css']，让构建器知道匹配 CSS 的导入需要保留。`,`重新检查生产构建，确认 Button.css 的规则仍在并实际应用到按钮。`],conclusion:`让元数据符合真实行为，才能保留必要导入；初始化或补丁文件也需准确标记，不能一律声明无副作用。`}},"frontend-agent-interaction-safety-040":{terms:[{term:`可访问角色与名称`,meaning:`辅助技术识别控件用途和名称的信息，例如角色 button、名称“提交”。`},{term:`行为断言`,meaning:`验证用户执行操作后观察到的结果，而不是依赖组件内部变量或函数名。`}],example:{scenario:`用户未填写必填昵称就点击提交，产品要求显示“请填写昵称”。`,steps:[`测试按 button 角色和“提交”名称找到按钮，模拟用户点击。`,`等待界面出现“请填写昵称”，再断言该提示可见，覆盖真正的操作结果。`,`内部 handleSubmit 即使重命名或改用另一种状态管理，只要交互结果一致，测试仍应通过。`],conclusion:`按钮操作与可见错误共同构成用户需求；仅检查回调被调用不能证明提示出现，仅检查私有状态也不能证明用户看到了结果。`}},"frontend-agent-interaction-safety-041":{terms:[{term:`CSP`,meaning:`内容安全策略，约束页面允许加载或执行哪些资源，可由 HTTP 响应头声明。`},{term:`Report-Only`,meaning:`只观察违规的部署模式，不单独执行阻断；可配置上报端点收集策略影响。`}],example:{scenario:`网站想限制脚本来源，但不确定现有统计脚本是否会被新规则误拦。`,steps:[`先通过 Content-Security-Policy-Report-Only 发送新策略，并正确配置报告接收。`,`统计脚本违反这份策略时产生违规线索；若没有其他拦截，这个报告头本身不会阻止它执行。`,`核对依赖并调整规则后，再用 Content-Security-Policy 强制执行需要的限制。`],conclusion:`观察模式回答“会影响什么”，强制模式才负责阻断。两种头可同时存在，不能因为收到了违规报告就认为攻击脚本已经被拦住。`}},"frontend-agent-interaction-safety-042":{terms:[{term:`微任务检查点`,meaning:`浏览器持续处理微任务队列的阶段，其中新加入的微任务也会继续被处理。`},{term:`渲染饥饿`,meaning:`计算长期占住执行机会，使输入处理与页面绘制迟迟无法得到时间。`},{term:`Worker`,meaning:`在主线程之外进行独立计算的工作线程，不能直接操作 DOM。`}],example:{scenario:`程序处理十万条记录，每处理 100 条就用 queueMicrotask 安排下一批，页面按钮一直点不动。`,steps:[`每批结束都加入下一批，微任务队列持续非空，浏览器仍在同一个检查点处理计算。`,`改成有限批次后让出到后续任务，给输入与渲染机会，或把独立计算放入 Worker。`,`主线程收到 Worker 的批次结果后更新界面，不再连续执行全部重计算。`],conclusion:`拆得更小还要真正让出控制；换成已兑现 Promise 的 then 仍是微任务，不能保证界面获得执行机会。`}},"frontend-agent-interaction-safety-043":{terms:[{term:`SSR`,meaning:`服务端渲染，服务器先生成页面 HTML，浏览器收到后即可展示初始内容。`},{term:`水合`,meaning:`客户端 React 接管已有服务端 HTML，并为它连接交互逻辑的过程。`},{term:`首帧一致性`,meaning:`水合时客户端第一次渲染的输出，要与服务端生成的内容相匹配。`}],example:{scenario:`服务器输出 light 主题，浏览器存储却记录 dark，客户端若立即读取就会输出不同内容。`,steps:[`客户端初始状态先采用服务器快照中的 light，使第一次渲染与现有 HTML 一致。`,`水合后的 Effect 再读取本地偏好，更新成 dark。`,`如果要减少主题切换闪烁，可提前把同一偏好提供给服务端和客户端，让两端都从 dark 开始。`],conclusion:`要统一首次渲染的输入或调整偏好应用时机；加 typeof window 分支仍会造成差异，隐藏警告也没有修复不一致。`}},"frontend-agent-interaction-safety-044":{terms:[{term:`reactive 代理`,meaning:`Vue 包装对象后的响应式访问入口，读取或修改其属性可以参与依赖追踪。`},{term:`toRef / toRefs`,meaning:`把响应式对象属性变成与原属性保持连接的 ref，便于通过 .value 访问。`}],example:{scenario:`普通 setup 用 reactive 包装 { count: 0 } 为 state，再解构出 count。`,steps:[`解构时读到 0，局部 count 保存这个值。`,`执行 state.count = 2 后，state.count 为 2，局部 count 仍为 0。`,`改从 toRefs(state) 解构 count，读取 count.value 就能跟随原属性变化。`],conclusion:`保留属性访问或创建关联 ref，才能继续跟踪变化；此处不讨论 defineProps 的编译转换规则。`}},"frontend-agent-interaction-safety-045":{terms:[{term:`Cache API`,meaning:`按名称保存请求与响应的浏览器缓存接口，同一源中的应用可能共享这一命名空间。`},{term:`waitUntil`,meaning:`把异步工作加入生命周期等待，让事件关联的工作完成后再继续后续阶段。`}],example:{scenario:`同源已有 quiz-static-v1、quiz-static-v2 和另一个应用的 media-v3 缓存。`,steps:[`新版答题应用激活，确认自己的 v1 已废弃、v2 应保留。`,`按应用前缀和版本筛选，只删除 quiz-static-v1，保留 v2 与不属于本应用的 media-v3。`,`把删除 Promise 交给激活事件的 waitUntil，等待清理结束。`],conclusion:`缓存名需表达归属，异步清理需纳入生命周期；激活不会自动清空旧缓存，升级时还要确认旧页面不再依赖被删资源。`}},"frontend-agent-interaction-safety-046":{terms:[{term:`乐观更新`,meaning:`请求尚未成功时先按预计结果更新界面，失败后再做补偿或同步。`},{term:`快照回滚`,meaning:`保存操作前的状态，失败时恢复它；并发修改下需要确认快照是否仍适合恢复。`},{term:`操作版本`,meaning:`标识哪次修改较新或属于哪个请求的信息，用于协调乱序完成的操作。`}],example:{scenario:`昵称最初为 Lin，M1 乐观改成 Lee，随后 M2 改成 Chen，且 M2 先得到服务端成功确认。`,steps:[`界面已显示 Chen；M1 此时才失败，旧快照中保存的仍是 Lin。`,`若直接恢复 M1 快照，会抹掉已经成功的 Chen，界面与服务端不一致。`,`选择同记录串行提交，或按操作身份和版本协调补偿，再按需要获取服务端最新状态。`],conclusion:`旧失败不能无条件撤销较新的成功；只取消查询，或按字段恢复但不检查后续写入，都不足以解决这个并发写竞态。`}},"frontend-agent-interaction-safety-047":{terms:[{term:`虚拟列表`,meaning:`只为当前可见附近的数据创建真实 DOM，用布局尺寸模拟整个长列表。`},{term:`缓冲项`,meaning:`在可见范围前后额外渲染少量行，减少滚动时来不及创建内容造成的空白。`},{term:`占位尺寸`,meaning:`用高度或偏移维持完整滚动范围，使未挂载的记录仍占有对应空间。`}],example:{scenario:`十万条记录每行固定 20px，400px 高的视口大约显示 20 行。`,steps:[`维持约 200 万像素的总滚动空间，但只挂载当前 20 行和前后少量缓冲。`,`向下滚动时更新可见索引和行偏移，移出窗口的行不再保留对应 DOM。`,`实际节点数量接近几十行，不再随十万条数据一起全部挂载。`],conclusion:`减少真实 DOM 才能针对题设瓶颈；隐藏十万行仍保留节点开销。动态行高、焦点和不在窗口中的内容访问需要另行处理。`}},"frontend-agent-interaction-safety-048":{terms:[{term:`nonce`,meaning:`每份响应生成的不可预测随机值，CSP 用它授权拥有匹配属性的可信脚本。`},{term:`授权边界`,meaning:`决定哪些代码属于服务器认可的执行内容；不能把用户注入的标签也当成可信脚本。`}],example:{scenario:`页面需要执行一段可信的内联启动脚本，同时展示用户提交的文字内容。`,steps:[`服务器为本次 HTML 生成随机值 N1，把它写入策略的 nonce 来源表达式，并赋给已知可信的启动脚本。`,`用户内容按不可信输入处理，不能扫描全部 script 后一律补上 N1。`,`下一份响应生成新的随机值 N2，并确保响应头和该份 HTML 中的可信标签匹配。`],conclusion:`nonce 是具体的执行许可，不是自动识别好坏脚本的标签；固定值、任意补授权或头与正文错配都会破坏预期设计。`}},"frontend-agent-interaction-safety-049":{terms:[{term:`层叠上下文`,meaning:`一组元素内部独立排序、对外作为整体参与前后遮挡排序的范围。`},{term:`z-index`,meaning:`参与所在层叠上下文内排序的层级值，子元素的数值不能直接改变祖先整体的次序。`}],example:{scenario:`同级父容器 A、B 都建立层叠上下文，层级分别为 1、2；A 内的弹窗层级是 99999。`,steps:[`先比较父级整体，B 的 2 排在 A 的 1 上方。`,`弹窗的 99999 只负责在 A 内部排序，不能把 A 的整个上下文抬到 B 上面。`,`根据布局需要调整合适的祖先层级，或把弹窗放到不会受 A 限制的位置，再检查遮挡。`],conclusion:`需要找到上下文边界，而不是不断增加子级数字；Portal 可帮助调整 DOM 位置，但最终层级仍由实际渲染位置和 CSS 决定。`}},"frontend-agent-interaction-safety-050":{terms:[{term:`判别联合`,meaning:`用共同字段的不同字面量值区分成员，例如 kind 为 loading、success 或 error。`},{term:`never`,meaning:`表示不应再有可能值的类型，可用来证明分支已覆盖所有成员，即穷尽检查。`}],example:{scenario:`switch 已处理 loading、success 两种状态，default 写 const rest: never = state。`,steps:[`两类都处理后，default 中没有剩余类型，赋给 never 可以通过检查。`,`增加 error 却漏写相应 case 时，default 中的 state 仍可能是 error。`,`它不能赋给 never，编译器报错；补全分支后错误消失。`],conclusion:`赋值约束可暴露遗漏，as never 则绕过证明。外部数据是否符合联合类型，仍需运行时验证。`}}};export{e as default};